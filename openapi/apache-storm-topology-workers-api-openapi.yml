openapi: 3.1.0
info:
  title: Apache Storm UI REST Cluster Topology Workers API
  version: '1.0'
  description: Minimal OpenAPI for the Apache Storm UI REST API exposing cluster, supervisor, nimbus, topology, and DRPC operations.
servers:
- url: http://localhost:8080
  description: Storm UI
tags:
- name: Topology Workers
paths:
  /api/v1/topology-workers/{id}:
    get:
      summary: Topology workers
      parameters:
      - $ref: '#/components/parameters/TopologyId'
      responses:
        '200':
          description: OK
      tags:
      - Topology Workers
components:
  parameters:
    TopologyId:
      name: id
      in: path
      required: true
      schema:
        type: string
x-generated-from: https://storm.apache.org/releases/current/STORM-UI-REST-API.html
x-generated-by: claude-crawl-2026-05-08
